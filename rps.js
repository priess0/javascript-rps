const choice;
var game = Math.floor(Math.random() * 3) + 1;
console.log(game);
if game === 1 {
  const choice === "rock";
} else if {
  const choice === "paper";
} else {
  const choice === "scissors";
}


    // set constant for rock
    // set constant for paper
    // set constant for scissors
    // set variable for game

    // have game use math.random to pick a number
    // assign
